import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

/// Note-taking app with FireBase and DDD architecture
class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: SizedBox(),
    );
  }
}
